using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum TileableObjectType { crop, animal, useableBuilding, fill, decor}

public enum ResourceType { resource, seed, baby, animal, consumeable, decor, useableBuilding, upgrade, crafting_station_food, crafting_staion_alcohol, food, alcohol }

public enum UseableBuildingType { crafting, storage }

public enum CraftingType { food, alcohol }
